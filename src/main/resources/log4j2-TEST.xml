<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Properties>
        <Property name="LOG_PATH">/home/dojun8ho/logs</Property>
        <Property name="DEFAULT_PATTERN">[%d{yyyy-MM-dd HH:mm:ss}] %5p ${sys:PID} --- [%15.15t] %-40.40c{1.} : %m%n%xwEx</Property>
        <Property name="COLOR_PATTERN">[%clr{%d{yyyy-MM-dd HH:mm:ss}}{faint}] %clr{%5p} %clr{${sys:PID}}{magenta} %clr{---}{faint} %clr{[%15.15t]}{faint} %clr{%-40.40c}{cyan} %clr{:}{faint} %m%n%xwEx</Property>
        <Property name="TEST_PATTERN">%d %5p [%c] %m%n</Property>
    </Properties>
	<Appenders>
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${COLOR_PATTERN}"/>
        </Console>

        <RollingFile name ="RollingFileAppender">
            <FileName>${LOG_PATH}/homes-java.log</FileName>
            <FilePattern>${LOG_PATH}/%d{yyyy}/%d{MM}/homes-java-%d{yyyy-MM-dd}-%i.log</FilePattern>
            <PatternLayout>
                <Pattern>${DEFAULT_PATTERN}</Pattern>
            </PatternLayout>
            <Policies>
                <SizeBasedTriggeringPolicy size="100MB"/>
                <TimeBasedTriggeringPolicy interval = "1" modulate = "true"/>
            </Policies>
        </RollingFile>
	</Appenders>

    <Loggers>
        <!-- log4jdbc 로그 설정 -->
        <Logger name="homes.log.sqlonly" level="ERROR" additivity="false">
			<appender-ref ref="Console"/>
        </Logger>
        <Logger name="homes.log.sqltiming" level="ERROR" additivity="false">
			<appender-ref ref="Console"/>
        </Logger>
        <Logger name="homes.log.resultset" level="ERROR" additivity="false">
			<appender-ref ref="Console"/>
        </Logger>

        <!-- ROOT -->
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFileAppender"/>
        </Root>
    </Loggers>
</Configuration>